{
  "name": "our-recipebook",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm -ws run typecheck",
    "lint": "npm -ws run lint",
    "test": "npm -ws run test",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "burnt": "^0.13.0"
  }
}
